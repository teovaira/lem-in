# Changelog

All notable changes to this project are documented here.

Format follows [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

---

## [Unreleased]

### Added
- Go module initialised (`lemin`)
- Package directory structure: `graph`, `parser`, `pathfinder`, `simulator`, `output`
- Makefile with `build`, `test`, `vet`, `race`, `clean`, `run` targets
- GitHub Actions CI workflow: vet → test (race) → build on every push and PR
- `README.md`, `CONTRIBUTING.md`, `CHANGELOG.md`, `AGENTS.md`
- Project implementation reference (`lem-in-reference.md`)
